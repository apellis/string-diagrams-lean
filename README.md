# string-diagrams-lean

A Lean 4 library for diagrammatic reasoning: typed diagram syntax, presentations by generators and relations, interpretations, and Lean-checked diagram rewriting.

## Contents

### Core

| Module | Contents |
| --- | --- |
| `StringDiagrams.Signature` | `Signature`: regions (0-cells), strand colours (1-cells), generators (2-cells) with typed boundaries and a parity; objects `Obj` and layers `Layer` (`id ⊗ g ⊗ id`) with well-formedness. Monoidal signatures are the case of a single region. |
| `StringDiagrams.Syntax` | The free 2-category: morphisms are well-typed lists of layers (`Diagram`), composed by concatenation; whiskering and retyping along equalities of objects (which never changes the underlying layers, so widths may be symbolic). |
| `StringDiagrams.Presentation` | `Presentation S R`: relations between `R`-linear combinations of diagrams. `Presentation.Presented` is the `R`-linear category obtained by quotienting by the tensor ideal generated by the relations and the (Koszul-signed) interchange law. `lin_rel_cast`, `diag_interchange`: relations and interchange at arbitrary whiskered positions. |
| `StringDiagrams.Interpretation` | Functors determined by images of layers; `Presentation.lift` (soundness): a functor killing all whiskered relations and interchange instances descends to an `R`-linear functor on the presented category. |
| `StringDiagrams.Whisker` | Whiskering `u ⊗ f ⊗ v` on the presented category (`Presentation.whisk`), linear and functorial. |
| `StringDiagrams.Interchange` | The interchange law for arbitrary diagrams in monoidal signatures: `(f ⊗ 1) ≫ (1 ⊗ g) = (-1)^{|f||g|} (1 ⊗ g) ≫ (f ⊗ 1)` (`Presentation.diag_interchange_diagrams`), derived from the single-generator instances. |
| `StringDiagrams.Horizontal` | The interchange law for arbitrary diagrams and general regions (2-categories): for `f : a ⟶ a'`, `g : b ⟶ b'` with `a` ending in the region where `b` starts (`Obj.Composable`), `(f ⊗ 1_b) ≫ (1_{a'} ⊗ g) = (-1)^{|f||g|} (1_a ⊗ g) ≫ (f ⊗ 1_{b'})` (`Presentation.diag_interchange_of_composable`), with the whiskered diagrams `Diagram.rwhisker`, `Diagram.lwhisker`. |
| `StringDiagrams.Bicategory` | Horizontal structure of presented 2-categories with general regions: right/left whiskering `Presentation.wRAt`, `Presentation.wL` and horizontal composition `Presentation.hcomp` on the presented category, with functoriality, strict associativity and unit laws, and the (Koszul-signed) interchange law. For even signatures, `P.Bicat` is a strict bicategory (Mathlib `Bicategory`, `Bicategory.Strict`) whose objects are regions, 1-morphisms well-formed words (`Presentation.Bicat.Hom`), and 2-morphisms the morphisms of `P.Presented`. |
| `StringDiagrams.Grading` | Gradings `deg : S.Gen → A`: degrees of diagrams; for homogeneous presentations the tensor ideal is homogeneous (`homogeneousComponent_mem_ideal`) and Hom-spaces decompose as internal direct sums `P.homDeg deg a b d` (`isInternal_homDeg`); endomorphism algebras are graded algebras (`gradedAlgebra`). Examples: free commuting dots (dot 2; `FreeDots.gradedAlgebra`), Temperley–Lieb (cup `t`, cap `−t`). |
| `StringDiagrams.Monoidal` | For even monoidal signatures (`Signature.IsEven`), `P.Presented` is a strict monoidal `R`-linear category (Mathlib `MonoidalCategory`, `MonoidalLinear`): `P.obj a ⊗ P.obj b = P.obj (a.tensor b)`, associators and unitors are `eqToIso`, `diag_whiskerRight`/`diag_whiskerLeft`. Examples: free commuting dots (`x R 1 0 ▷ FD.obj R 1 = x R 2 0`; commutativity of neighbouring dots from `whisker_exchange`, `x_mul_x_comm_two`), Temperley–Lieb (one strand is self-dual: `ExactPairing` from cup and cap, `η ≫ ε = δ`). |
| `StringDiagrams.Derivation` | Local super-derivations (diagrammatically local differentials, as in Ellis–Qi, arXiv:1504.01712v2, §§2.2–3.1): values `δ g` on generators of parity `|g| + 1` extend by the super Leibniz rule (`derivFree`); for monoidal presentations with homogeneous relations killed by `d`, the derivation descends to the presented category (`LocalDerivation.derivFree_mem_ideal`, `Presentation.deriv`), satisfies `d(f g) = d(f) g + (-1)^{|f|} f d(g)` (`deriv_mul`), commutes with whiskering, and `d² = 0` follows from its vanishing on generators (`deriv_deriv_eq_zero`). Examples: odd dots with `d(x) = x²`; the exterior example with `d(x) = 1`. |
| `StringDiagrams.Generation` | Induction principles: morphisms are linear combinations of diagrams, diagrams are composites of layers (`Presentation.hom_induction_layers`). |
| `StringDiagrams.LocalInterpretation` | Representations by local operators (`LocalInterpretation`): objects go to modules `M (κ a)` indexed by a function of the object, layers act by local operators on an ambient module. The image of any diagram is the composite of its layer operators (`functor_map_hom`, and `functor_map_transport` along arbitrary equalities of indices); soundness reduces to the vanishing of a linear evaluation of each whiskered relation (`respects_of`, `evalW`), and the interchange part to commutation of generators at disjoint positions with the Koszul sign (`evalW_interchange_eq_zero`). Special cases: one module for all objects (`uniform`), index-preserving componentwise operators on `Π i, M i` (`pi`, `pi_functor_map_transport`). Over `ℤ`: `Presentation.int_smul_eq_zsmul`, `Presentation.hom_induction_int`. Used by the polynomial representation of free commuting dots (`uniform`) and the Temperley–Lieb representation. |

### Biadjunctions (any Mathlib bicategory)

| Module | Contents |
| --- | --- |
| `StringDiagrams.Biadjunction.Basic` | `Biadjunction f g` (`f ⊣⊢ g`: Mathlib adjunctions `f ⊣ g` and `g ⊣ f`), `id`, `comp`, `symm`; the right and left mates `(f ⟶ f') ≃ (g' ⟶ g)`; `IsCyclic` (the two mates agree); right and left traces (bubbles) in `𝟙 a ⟶ 𝟙 a`, `𝟙 b ⟶ 𝟙 b`. |
| `StringDiagrams.Biadjunction.Conjugate` | Sliding a 2-morphism through a unit or counit (`unit_comp_whiskerRight_eq`, `whiskerLeft_comp_counit_eq`), uniqueness of conjugates, conjugates of whiskerings for composite adjunctions. |
| `StringDiagrams.Biadjunction.Cyclic` | Mates reverse composition; cyclic 2-morphisms are closed under composition, inverses, whiskering, horizontal composition and mates; coherence isomorphisms, units and counits are cyclic (with explicit mates). |
| `StringDiagrams.Biadjunction.Linear` | `LocallyPreadditive`, `LocallyLinear` bicategories; mates as linear equivalences; cyclic 2-morphisms form a submodule; linearity of traces. |
| `StringDiagrams.Biadjunction.Trace` | `tr(α ≫ β) = tr(β ≫ doubleMate α)` in general (`rightTrace_comp`), hence `tr(α ≫ β) = tr(β ≫ α)` when either factor is cyclic; traces versus mates; partial traces for composite biadjunctions. |
| `StringDiagrams.Biadjunction.Pivotal` | `Pivotal B` (chosen biadjoints, every 2-morphism cyclic); rotation `rotate` is a contravariant equivalence, `rotate (rotate α)` is conjugation by the natural `f ≅ dual (dual f)`; traces are cyclic. |
| `StringDiagrams.Biadjunction.Monoidal` | Monoidal case via `MonoidalSingleObj`: `ExactPairing` as adjunction, bidualities, mates agree with Mathlib's `rightAdjointMate`/`leftAdjointMate`, traces in `End (𝟙_ C)`. |
| `StringDiagrams.Biadjunction.Zigzag` | Adjunctions in `P.Bicat` (`adjunctionOfZigzag`, `adjunctionOfRels`) and `ExactPairing`s in the monoidal case (`exactPairingOfZigzag`, `exactPairingOfRels`) from cup/cap classes satisfying zigzag equations, or directly from zigzag relations of the presentation. |
| `StringDiagrams.Biadjunction.Words` | Colour dualities compatible with regions; dual words `(w.map dual).reverse`; biadjunctions for all well-formed words by composition, with nested units/counits, from biadjunctions of single colours. |
| `StringDiagrams.Biadjunction.Presented` | Biadjunctions from four cups and caps; pitchfork (sliding) lemmas; mates of diagram classes as rotated diagrams (`rightMate_diag`, `isCyclic_diag_iff`); rotation invariance: if every generator is cyclic, every 2-morphism is (`Presentation.isCyclic_of_generators`), packaged as `pivotalOfGenerators : Pivotal P.Bicat`. |
| `StringDiagrams.Biadjunction.PivotalExtension` | The pivotal extension of a presentation: added cup/cap generators for every colour and zigzag relations (`Presentation.pivotal`); for an involution on colours, cups and caps are automatically cyclic, so a pivotal structure only needs cyclicity of the original generators. |
| `StringDiagrams.Biadjunction.NestedCups` | Explicit nested cups and caps `1 ⟶ w w*`, `w* w ⟶ 1`, `1 ⟶ w* w`, `w w* ⟶ 1` for every well-formed word (`ColourCupCapDiagrams.cupW`, `capW`, `cup'W`, `cap'W`), with layer lists for `c :: w` and for concatenations `u ++ w` (symbolic words); the units and counits of the word biadjunctions are exactly their classes (`Presentation.biadj_left_unit_eq_wordCup`, …), and their zigzag identities are derived (`diag_leftZigzag_wordCup_wordCap`, …). Rotation of diagrams with arbitrary boundaries: mates are the classes of explicit rotated diagrams (`rightMate_biadj_diag`, `leftMate_biadj_diag`, `isCyclic_biadj_diag_iff`), sliding along nested cups and caps (`diag_wordCup_comp_rwhisker`, …). Cyclicity of a generator as the relation `genRotR g = genRotL g` (`isCyclic_gen_of_rel`, `pivotalOfGenRot`); the cyclic pivotal extension `Presentation.pivotalCyclic` imposes it for all generators and is pivotal with no further hypotheses (`pivotalCyclicStructure`). |
| `StringDiagrams.Biadjunction.PivotalInclusion` | The inclusion of diagrams into the pivotal extension (generators `g ↦ .gen g`) as a functor of free 2-categories, and its descent `Presentation.toPivotalLift : P.Presented ⥤ Q.Presented` (an `R`-linear functor, via `Presentation.lift`) into any presentation `Q` of the pivotal extension containing the relations of `P`: `P.pivotal D`, its extensions `addRels`, and `P.pivotalCyclic E`; values on diagrams, layers and generators (`toPivotalLift_diag`, `toPivotalLift_genDiag`). |

### Monoidal supercategories and 2-supercategories

Following J. Brundan, A. P. Ellis, *Monoidal supercategories* ([arXiv:1603.05928v3](https://arxiv.org/abs/1603.05928)). Mathlib's `MonoidalCategory` and `Bicategory` contain the ordinary interchange law, so the super versions are separate `Prop` classes over Mathlib's `MonoidalCategoryStruct` and a data class `BicategoryStruct`.

| Module | Contents |
| --- | --- |
| `StringDiagrams.Super.Functor` | Definition 1.1(ii)–(iii), (v): superfunctors (`Supercategory.IsSuperfunctor`: linear, parity-preserving), supernatural transformations given by their homogeneous components with the sign rule `F f ≫ x_{p} = (-1)^{p|f|} x_{p} ≫ G f` (`Supercategory.SuperNatTrans`), vertical composition, even supernatural transformations as natural transformations with even components; the Koszul sign `koszulSign p q`. |
| `StringDiagrams.Super.Monoidal` | Definition 1.4: `MonoidalSupercategory R C` (Mathlib's monoidal axioms except `tensor_comp`, functoriality of whiskering, the super interchange law `f ▷ Y ≫ X' ◁ g = (-1)^{|f||g|} (X ◁ g ≫ f ▷ Y')`, linear and even whiskering, even coherence maps), `IsStrict`; the super interchange law (1.1) for the paper's tensor product `superTensorHom f g = X ◁ g ≫ f ▷ Y'` (`superTensorHom_comp_superTensorHom`) and for Mathlib's `tensorHom`; without odd morphisms, monoidal supercategories are Mathlib monoidal linear categories and conversely (`toMonoidalCategory`, `ofMonoidalCategory`); monoidal superfunctors and monoidal natural transformations (Definition 1.4(ii)–(iii)). |
| `StringDiagrams.Super.Bicategory` | Definitions 2.1–2.2: `BicategoryStruct` (the data of Mathlib's `Bicategory`), `TwoSupercategory R B` (Mathlib's bicategory axioms except `whisker_exchange`, the super interchange law, linear and even whiskering, even coherence maps), `BicategoryStruct.Strict`; the super interchange law `(vu) ∘ (yx) = (-1)^{|u||y|} (v ∘ y)(u ∘ x)` (`hcomp_comp_hcomp`); without odd 2-morphisms, a 2-supercategory is a Mathlib bicategory (`toBicategory`, `toBicategory_strict`). |
| `StringDiagrams.Super.Presented` | For parity-homogeneous presentations with odd generators: `P.Presented` is a strict monoidal supercategory for monoidal signatures (`Presentation.monoidalSupercategory`, `Presentation.isStrict`) and `P.Bicat` a strict 2-supercategory for general regions (`Presentation.twoSupercategory`, `Presentation.Bicat.instStrict`); the super interchange law for homogeneous morphisms (`wR_comp_wL_super`, `wRAt_comp_wL_super`). For even signatures the induced Mathlib structures are the existing instances (`toMonoidalCategory_eq`, `toBicategory_eq`, by `rfl`). |
| `StringDiagrams.Super.FunctorCategory` | Example 1.2(iv): the supercategory `ℋom(A, B)` of superfunctors (`Supercategory.Superfunctor R A B`) and supernatural transformations, with its `Preadditive`, `Linear` and `Supercategory` instances (the morphisms of parity `p` are the homogeneous supernatural transformations, `Superfunctor.mem_parity_iff_isHomogeneous`); horizontal composition `(yx)_λ = y_{Hλ} ∘ G x_λ` (`Superfunctor.hcomp`, `hcomp_total`) and the super interchange law (`Superfunctor.whisker_exchange`, `Superfunctor.hcomp_comp_hcomp`). |
| `StringDiagrams.Super.SCat` | Section 2: supercategories, superfunctors and supernatural transformations form a strict 2-supercategory `SCat R` (instances `TwoSupercategory R (SCat R)`, `BicategoryStruct.Strict (SCat R)`, with identity coherence maps); likewise `PiSCat R` for Π-supercategories (Section 3). The product `⊠` of supercategories is not formalized: as in `StringDiagrams.Super.Bicategory`, Definitions 2.1–2.2 are used in their equivalent unpacked (whiskering) form. The superbimodule 2-supercategory `𝔖𝔅𝔦𝔪` (Sections 2–3) is not formalized. |
| `StringDiagrams.Super.UnderlyingBicategory` | The underlying bicategory of even 2-morphisms of a 2-supercategory (`Underlying2 R B`, a Mathlib `Bicategory`), used to transport coherence lemmas (`TwoSupercategory.unitors_equal`, `leftUnitor_comp`, `rightUnitor_comp`, `whiskerLeft_rightUnitor`, `pentagon_inv`); naturality of the coherence maps for all 2-morphisms; the super interchange law with an even factor (`whisker_exchange_of_even_left`, `whisker_exchange_of_even_right`). |
| `StringDiagrams.Super.TwoFunctor` | Definition 2.2(ii)–(iv): 2-superfunctors with coherence maps `c` (`mapComp`) and `i` (`mapId`) and the three coherence diagrams (`TwoSuperfunctor`, `TwoSuperfunctor.id`, `TwoSuperfunctor.mapComp_naturality`: `c` is supernatural on `⊠`), strict 2-superfunctors (`TwoSuperfunctor.IsStrict`); 2-natural transformations `(X, x)` (`TwoNatTrans`, strong: `TwoNatTrans.IsStrong`); supermodifications (`Supermodification`) and the supercategory `ℋom(ℝ, 𝕊)` of 2-natural transformations and supermodifications (instances on `TwoNatTrans F G`). Superequivalences in a 2-supercategory (`TwoSupercategory.IsSuperequivalence`, Definition 2.2(i)); in `SCat` they are the superequivalences of Definition 1.1(iv) (`SCat.isSuperequivalence_iff`). |
| `StringDiagrams.Super.TwoFunctorComp` | Composite of 2-superfunctors (`TwoSuperfunctor.comp`, `c_{𝕊ℝ} = 𝕊(c_ℝ) ∘ c_𝕊`); the identity and vertical composite of 2-natural transformations (`TwoNatTrans.id`, `TwoNatTrans.vcomp`), obtained from Mathlib's oplax transformations of the underlying bicategories (`TwoSuperfunctor.toOplax`, `TwoNatTrans.toOplaxTrans`, `TwoNatTrans.ofOplaxTrans`). |
| `StringDiagrams.Super.DrinfeldCenter` | Definition 2.3: the Drinfeld center of a 2-supercategory, the strong 2-natural transformations `𝕀 ⇒ 𝕀` and supermodifications (`DrinfeldCenter R B`), a monoidal supercategory with `(X ⊗ Y)_λ = X_λ Y_λ`, `(α ⊗ β)_λ = α_λ β_λ` (instance `MonoidalSupercategory R (DrinfeldCenter R B)`). Lemma 3.2: `(π, β)` is an object of the Drinfeld center (`PiTwoSupercategory.centerObj`), with an odd isomorphism `ζ : (π, β) ≅ 1` (`centerζ`) and an even isomorphism `ξ : (π, β) ⊗ (π, β) ≅ 1` (`centerξ`, `centerξ_hom_eq`). The braiding and the strictness of the Drinfeld center of a strict 2-supercategory are not formalized. |

### Π-supercategories and Π-envelopes

Following Brundan–Ellis, *Monoidal supercategories* ([arXiv:1603.05928v3](https://arxiv.org/abs/1603.05928)), §§1.3–1.4, 3–5.

| Module | Contents |
| --- | --- |
| `StringDiagrams.Super.Parity` | Parity components `proj p f` of morphisms in a supercategory, `f = f₀ + f₁` (`proj_add_proj`), induction via homogeneous morphisms, the twist `f₀ + (-1)^p f₁` (compatible with composition, `twist_comp`); inverses of homogeneous isomorphisms are homogeneous (`inv_mem`). |
| `StringDiagrams.Super.Supernatural` | Homogeneous supernatural transformations (`IsSupernatural R p x`, the parity-`p` components of a `SuperNatTrans`: `isSupernatural_iff`), vertical composition and whiskering; faithful superfunctors reflect parity; evenly dense superfunctors and superequivalences (Definition 1.1(iv)); a full, faithful, evenly dense superfunctor is a superequivalence (`Superequivalence.ofFullyFaithful`). |
| `StringDiagrams.Super.PiCategory` | Definition 1.6: Π-categories `(A, Π, ξ)`, Π-functors `(F, β_F)` (identity, composition `β_{GF} = G β_F ∘ β_G F`, `Π` with `β_Π = -1`), Π-natural transformations. |
| `StringDiagrams.Super.Pi` | Definition 1.7: Π-supercategories `(A, Π, ζ)`; construction from odd isomorphisms `ζ_λ : Πλ ≅ λ` (`PiSupercategory.ofIso`, `Π` is then forced: `pi_map_eq`); `ξ := ζζ` with both formulas of (1.4), `Πζ = -ζΠ` (Lemma 3.2(iii)), `ξΠ = Πξ` (Corollary 3.3(i)); `β_F := -ζ_B F ζ_A⁻¹` and Corollary 3.3(ii)–(iv) (`β_comm`, `β_naturality_supernatural`, `β_comp`, `β_id`, `β_pi`). |
| `StringDiagrams.Super.PiTwo` | Definition 3.1: Π-2-supercategories (`PiTwoSupercategory R B`: 1-morphisms `π_λ`, odd 2-isomorphisms `ζ_λ : π_λ ≅ 1_λ`); `Π-SCat` is a strict Π-2-supercategory (`PiSCat.instPiTwoSupercategory`); each `ℋom(λ, μ)` is a Π-supercategory with `Π = π_μ -` or `Π = - π_λ` (`PiTwoSupercategory.homPiLeft`, `homPiRight`). Lemma 3.2, for arbitrary (not necessarily strict) Π-2-supercategories, with the unitors and associators made explicit: `β_F := F ζ_μ ∘ ρ ∘ λ⁻¹ ∘ ζ_λ⁻¹ F` equals the paper's `-ζ_μ F ζ_λ⁻¹` (`β_hom_eq_neg_hcomp`), is even (`β_hom_mem`) and supernatural (`β_naturality`, (3.1); `β_isSupernatural`); (i) `β_comp`, (ii) `β_id`, (iii) `pi_whisker_ζ`, `β_pi`, (iv) `ξ_hom_mem`, `ξ_comm`. In `Π-SCat` the components of `β` and `ξ` are those of `StringDiagrams.Super.Pi` (`PiSCat.β_app_zero`, `PiSCat.ξ_app_zero`), and Corollary 3.3 is re-derived from Lemma 3.2 (`PiSCat.pi_map_ζ`, `PiSCat.ξ_pi`, `PiSCat.β_comm`, `PiSCat.β_naturality_supernatural`, `PiSCat.β_comp`, `PiSCat.β_id`, `PiSCat.β_pi`). |
| `StringDiagrams.Super.Envelope` | Definition 1.10: the Π-envelope `Envelope R C` (objects `Πᵃ λ`, `Hom(Πᵃλ, Πᵇμ) = Π^{a+b} Hom(λ, μ)`) as a Π-supercategory; `J : A → A_π`. Lemma 4.1: `J` is a superequivalence iff `A` is Π-complete (`J_evenlyDense_iff`, `superequivalenceJ`, `piComplete_of_superequivalence`). Lemma 4.2: extension `F̃` of superfunctors with `J ⋙ F̃ = F` (`extend`, `J_comp_extend`) and of supernatural transformations by (4.2), uniquely (`extendNat`, `eq_extendNat`). Theorem 4.3: restriction along `J` is bijective on supernatural transformations of each parity (`restrict_bijective`), extension respects identities, composition and linear combinations, and every superfunctor `A_π → B` is evenly isomorphic to the extension of its restriction (`extendRestrictIso`). |
| `StringDiagrams.Super.Underlying` | Definition 1.1(v): the underlying category of even morphisms; for a Π-supercategory it is a Π-category with `ξ = ζζ`, superfunctors give Π-functors `(F̲, β_F)` and even supernatural transformations give Π-natural transformations: the functor `E₁ : Π-SCat → Π-Cat` of (5.1). |
| `StringDiagrams.Super.Associated` | (5.2): the Π-supercategory `Â` associated to a Π-category (`Associated R C`), `D₁` on Π-functors (functorial: `map_id`, `map_comp`) and on Π-natural transformations. Lemma 5.1: `E₁(D₁ A)` is isomorphic to `A` by mutually inverse functors commuting strictly with `Π` and `ξ` (`unit`, `counit`, `ξ_unit`, `unitPiFunctor`), and `T_A : D₁(E₁ A) → A` is an isomorphism of supercategories carrying `ζ` to `ζ`, natural in `A` (`T`, `Tinv`, `T_comp_Tinv`, `Tinv_comp_T`, `T_map_ζ`, `T_naturality`). See the erratum below. |
| `StringDiagrams.Super.MonoidalEnvelope` | Definition 1.16: the Π-envelope of a monoidal supercategory is a monoidal supercategory (`Envelope.monoidalSupercategory`) with the paper's sign formula `f_a^b ⊗ g_c^d = (-1)^{a\|g\|+\|f\|d+ad+ac} (f ⊗ g)_{a+c}^{b+d}` (`toHom_superTensorHom`); `π = Π¹1`, `ζ = (1_1)_1^0`; `J` is a monoidal superfunctor (`monoidalJ`). |
| `StringDiagrams.Super.MonoidalPi` | The underlying category of a monoidal supercategory is a Mathlib monoidal linear category (used to transport coherence lemmas). Definition 1.12: monoidal Π-supercategories; `Π = π ⊗ -` (`toPiSupercategory`); the half-braiding `β` with naturality, (1.6), (1.7), `β_π = -1`, and `ξ = (l_1 = r_1)(ζ ⊗ ζ)` with (1.8). Definition 1.14: monoidal Π-categories (with Mathlib's `HalfBraiding`), monoidal Π-functors and Π-natural transformations; the functor (2) of (1.9) on objects (`toMonoidalPiCategory`) and on monoidal superfunctors (`toMonoidalPiFunctor`). The monoidal Π-envelope is a monoidal Π-supercategory. The first half of Theorem 1.15: first half in `StringDiagrams.Super.MonoidalUniversal`, second half in `StringDiagrams.Super.MonoidalAssociated`. |
| `StringDiagrams.Super.EnvelopeAdjunction` | Theorem 4.3 (the first half of Theorem 1.9) as a superequivalence of supercategories of superfunctors `ℋom(A, νB) → ℋom(A_π, B)`, `F ↦ F̃`, `x ↦ x̃` (`Envelope.extendHom`, `Envelope.extendHomSuperequivalence`). |
| `StringDiagrams.Super.MonoidalUniversal` | Theorem 1.15, first half, proved directly for monoidal supercategories along the lines of Lemma 4.7: a monoidal superfunctor `F : A → B` into a monoidal supercategory with a Π-supercategory structure (for a monoidal Π-supercategory, `Π = π ⊗ -`: `Envelope.extendMonoidalPi`) extends to a monoidal superfunctor `F̃ : A_π → B` with coherence map `(ζᵃ ⊗ ζᵇ) ≫ c ≫ (ζ^{a+b})⁻¹` (`Envelope.extendMonoidal`, `F̃ J = F`: `Envelope.extendMonoidal_μIso_J`); monoidal natural transformations extend uniquely (`Envelope.extendMonoidalNatTrans`, `Envelope.extendMonoidalNatTrans_unique`); restriction along `J` is bijective (`Envelope.restrictMonoidal_bijective`) and extension is functorial (`Envelope.extendMonoidalNatTrans_id`, `Envelope.extendMonoidalNatTrans_comp`); every monoidal superfunctor `A_π → B` is isomorphic to the extension of its restriction by mutually inverse even monoidal natural transformations (`Envelope.extendRestrictMonoidalHom`, `Envelope.extendRestrictMonoidalInv`, `Envelope.extendRestrictMonoidal_hom_inv`). |
| `StringDiagrams.Super.TwoEnvelope` | Definition 4.4: the Π-envelope `TwoEnvelope R B` of a 2-supercategory, with morphism supercategories `Envelope R (a ⟶ b)` and horizontal composition (4.4) (`TwoEnvelope.toHom_hcomp`; the associator sign check `TwoEnvelope.associator_sign_eq`). Lemma 4.5: it is a 2-supercategory (`TwoEnvelope.twoSupercategory`, `TwoEnvelope.toHom_super_interchange`). `π_λ = Π¹1_λ`, `ζ_λ = (1)_1^0`, and `ξ_λ = ζ_λζ_λ = -1` (`TwoEnvelope.toHom_ξ`). Lemma 4.6: `𝕁` induces superequivalences on all morphism supercategories iff `𝔄` is Π-complete (`TwoEnvelope.superequivalenceJ_iff`). |
| `StringDiagrams.Super.TwoEnvelopeUniversal` | Lemma 4.7 and Theorem 4.9, for 2-supercategories `𝔅` whose morphism supercategories carry Π-supercategory structures. The strict 2-superfunctor `𝕁 : 𝔄 → 𝔄_π` (`TwoEnvelope.twoJ`). Lemma 4.7(i): the extension `ℝ̃` of a 2-superfunctor with coherence map `c̃ = (-1)^{ab} (ζ^{a+b})⁻¹ ∘ c ∘ (ζᵇζᵃ)` (`TwoEnvelope.extend`, `TwoEnvelope.extendComp`; `ℝ = ℝ̃𝕁`: `TwoEnvelope.extend_map_J`, `TwoEnvelope.extend_map₂_J`, `TwoEnvelope.extendComp_J`, `TwoEnvelope.extend_mapId`). Lemma 4.7(ii): the extension `(X̃, x̃)` of a 2-natural transformation by (4.6) and its uniqueness (`TwoEnvelope.extendTwoNatTrans`, `TwoEnvelope.extendTwoNatTrans_unique`). Theorem 4.9: restriction along `𝕁` is a bijection on 2-natural transformations (`TwoEnvelope.extendTwoNatTransEquiv`); with the extension of supermodifications of Remark 4.10 (`TwoEnvelope.extendSupermodification`) it is a superequivalence `ℋom(ℝ, 𝕊) → ℋom(ℝ̃, 𝕊̃)` (`TwoEnvelope.extendHomSuperequivalence`); every 2-superfunctor `𝕋 : 𝔄_π → 𝔅` is isomorphic to `(𝕋𝕁)~` by strong 2-natural transformations with identity 1-morphisms and mutually inverse components (`TwoEnvelope.restrict`, `TwoEnvelope.extendRestrictNatTrans`, `TwoEnvelope.extendRestrictNatTransInv`, `TwoEnvelope.extendRestrictX_comp_inv`). |
| `StringDiagrams.Super.TwoEnvelopeEquivalence` | Theorem 4.9 with composites (`TwoSuperfunctor.comp`, `TwoNatTrans.vcomp`, `TwoNatTrans.id`): `𝕁` is strict (`TwoEnvelope.twoJ_isStrict`), `ℝ = ℝ̃𝕁` as 2-superfunctors (`TwoEnvelope.twoJ_comp_extend`), `TwoEnvelope.restrict 𝕋 = 𝕋𝕁` (`TwoEnvelope.restrict_eq_comp`), functoriality of the extension of 2-natural transformations (`TwoEnvelope.extendTwoNatTrans_id`, `TwoEnvelope.extendTwoNatTrans_vcomp`), and the two composites of `(𝕋𝕁)~ ⇒ 𝕋 ⇒ (𝕋𝕁)~` are isomorphic to identities by even invertible supermodifications (`TwoEnvelope.extendRestrictCounitIso`, `TwoEnvelope.extendRestrictUnitIso`). |
| `StringDiagrams.Super.TwoEnvelopePi` | `𝔄_π` is a Π-2-supercategory (`TwoEnvelope.instPiTwoSupercategory`); the Π-supercategories `ℋom(λ, μ)` of a Π-2-supercategory, `Π = π_μ -` (`PiTwoSupercategory.homPiLeft`); Lemma 4.7(i) for Π-2-supercategories (`TwoEnvelope.extendPi`), with the paper's formula `c̃ = m_{b,a} c ∘ π^b (β^a)⁻¹`, `m_{1,1} = -ξ`, checked in all four cases (`TwoEnvelope.extendComp_J`, `TwoEnvelope.extendComp_zero_one`, `TwoEnvelope.extendComp_one_zero`, `TwoEnvelope.extendComp_one_one`). |
| `StringDiagrams.Super.TwoEnvelopeFunctor` | The 2-functor `-_π` of (4.7) on 2-superfunctors (`TwoEnvelope.mapPi`, `c_π = (c)_{a+b}^{a+b}`) and 2-natural transformations (`TwoEnvelope.mapPiNatTrans`), and on supermodifications as in Remark 4.10 (`TwoEnvelope.mapPiSupermodification`). |
| `StringDiagrams.Super.UnitEnvelope` | Example 4.8 (over any commutative ring): `I_π` has two objects (`UnitEnvelope.objEquiv`), `Hom(Πᵃ, Πᵇ)` is free with basis `1_a^b` (`UnitEnvelope.homEquiv`), `1_b^d ⊗ 1_a^c = (-1)^{(a+c)b} 1_{a+b}^{c+d}` (`UnitEnvelope.toHom_one_tensor_one`); the extension `F̃ : I_π → SVec` of `I → SVec` (`UnitEnvelope.extendSVec`) sends `1_a^b` to the identity function (`UnitEnvelope.extendSVec_map_one`), has coherence maps `1 ⊗ 1 ↦ 1` (`UnitEnvelope.extendSVec_μ_one_tmul_one`), and `id_b^d ⊗ id_a^c` sends `1 ⊗ 1 ↦ (-1)^{(a+c)b} 1 ⊗ 1` (`UnitEnvelope.superTensorHom_extendSVec_one`). |
| `StringDiagrams.Super.MonoidalAssociated` | Theorem 1.15, second half (the functor (2) of (1.9) is an equivalence `Π-SMon ≃ Π-Mon`; the one-object case of (5.5) and Lemma 5.4): a monoidal Π-category is a Π-category with `Π = π ⊗ -` (`MonoidalPiCategory.toPiCategory`; `ξΠ = Πξ` is derived from (1.8), and (1.6) from (1.7): `MonoidalPiCategory.β_unit`); its associated supercategory `Â` is a monoidal supercategory (`Associated.monoidalSupercategory`) and a monoidal Π-supercategory (`Associated.instMonoidalPiSupercategory`). `E ∘ D = I`: `Associated.unitMonoidalPiFunctor` (from `Associated.β_hom_eq`, `Associated.ξ_hom_eq`), `Associated.unit_comp_map`, `Associated.jIso_mapMonoidal_hom`. `D` on monoidal Π-functors: `MonoidalPiFunctor.toPiFunctor`, `Associated.mapMonoidal`. `D ∘ E ≅ I`: `Associated.Tmon` is an isomorphism (`Tmon_comp_TmonInv`, `TmonInv_comp_Tmon`), a strict monoidal superfunctor (`TmonMonoidal`), carries `ζ` to `ζ` (`Tmon_map_ζ`) and is natural (`Tmon_naturality`). Corrected odd–odd tensor rule: `Associated.superTensorHom_odd_odd` (erratum 2 below). |
| `StringDiagrams.Super.AssociatedFunctorial` | (5.3) and Theorem 5.3 (with Lemma 5.1: the second bullet of Theorem 1.9): `E₁` and `D₁` on 2-morphisms preserve identities, vertical composition and whiskering (`Underlying.natTrans_id`, `Underlying.natTrans_comp`, `Underlying.natTrans_whiskerLeft`, `Underlying.natTrans_whiskerRight`, `Associated.mapNatTrans`, `Associated.mapNatTrans_id`, `Associated.mapNatTrans_comp`, `Associated.mapNatTrans_whiskerLeft`, `Associated.mapNatTrans_whiskerRight`); both are strict on `π`, `β`, `ξ` (`PiSCat.underlying_piFunctor_β`, `PiSCat.underlying_ξ`, `Associated.pi_eq_map_pi`, `Associated.β_map`); `E₁ ∘ D₁ = I` on 2-morphisms (`Associated.natTrans_mapNatTrans`); `T` is Π-2-natural (`Associated.T_map_mapNatTrans`, `Associated.β_T`). The strict 2-category `Π-ℭ𝔞𝔱` is not constructed. |
| `StringDiagrams.Super.PiTwoCategory` | Definition 5.2, for (not necessarily strict) Mathlib bicategories: `PreadditiveBicategory`, `LinearBicategory`, `PiTwoCategory` (the hom categories are Π-categories, `PiTwoCategory.homPiCategory`, with `ξΠ = Πξ` derived: `PiTwoCategory.ξHom_pi`), `PiTwoFunctor` (on Mathlib pseudofunctors), `PiTwoFunctor.IsPiTwoNatural` (on Mathlib oplax natural transformations). `E₂` of (5.4): `Underlying2.instPiTwoCategory`, `TwoSuperfunctor.toPseudofunctor`, `TwoSuperfunctor.toPiTwoFunctor` (axioms `TwoSuperfunctor.jβ_comm`, `TwoSuperfunctor.jξ_comm`); `𝔼₂` of (5.6): `TwoNatTrans.toOplaxTrans` is Π-2-natural (`TwoNatTrans.isPiTwoNatural`). |
| `StringDiagrams.Super.AssociatedTwo` | (5.5), `D₂` on objects: the Π-2-supercategory `𝔄̂` associated to a Π-2-category (`Associated2 R B`, `Associated2.twoSupercategory`, `Associated2.instPiTwoSupercategory`), with `β̂ = β` and `ξ̂ = ξ` (`Associated2.β_hom_eq`, `Associated2.ξ_hom_eq`) and the corrected odd–odd horizontal rule (`Associated2.hcomp_odd_odd`, errata 1–2 below). |
| `StringDiagrams.Super.AssociatedTwoT` | Lemma 5.4 on objects: `E₂ ∘ D₂ = I` (`Associated2.unit`, `Associated2.unit_map₂_bijective`, `Associated2.unitPiTwoFunctor`) and `D₂ ∘ E₂ ≅ I` via the 2-superfunctor `Associated2.T`, bijective on 2-morphisms (`Associated2.T_map₂_bijective`) and carrying `ζ` to `ζ` (`Associated2.T_map₂_ζ`). |
| `StringDiagrams.Super.AssociatedTwoMap` | (5.5), `D₂` on Π-2-functors (`PiTwoFunctor.homPi`, `PiTwoFunctor.mapCompLeft_isPiNatural`, `PiTwoFunctor.mapCompRight_isPiNatural`, `PiTwoFunctor.mapTwo`); Lemma 5.4 on morphisms: `E₂ ∘ D₂ = I` (`PiTwoFunctor.jIso_mapTwo_hom`) and naturality of `𝕋` (`Associated2.T_map₂_mapTwo`, `Associated2.T_map₂_mapTwo_mapComp`, `Associated2.T_map₂_mapTwo_mapId`). |
| `StringDiagrams.Super.AssociatedTwoNat` | (5.6) and Theorem 5.5 on 2-morphisms: `𝔻₂` on Π-2-natural transformations (`PiTwoFunctor.natHom_isPiNatural`, `PiTwoFunctor.mapTwoNat`), `𝔼₂ ∘ 𝔻₂ = 𝕀` (`PiTwoFunctor.toOplax_mapTwoNat_app`, `PiTwoFunctor.toOplax_mapTwoNat_naturality`), naturality of `𝕋` in 2-morphisms (`Associated2.T_map₂_mapTwoNat`). The 2-categories `Π-2-𝔖ℭ𝔄𝔗`, `Π-2-ℭ𝔄𝔗`, Corollary 5.6 and Remark 5.7 are not formalized. |

**Erratum (§5, proof of Lemma 5.1).** With `ξ := ζζ` as in Definition 1.7 and (5.1), the composition rule `ĝ ∘ f̂ := ξ_ν ∘ Πg ∘ f` for odd `f̂, ĝ` in the associated supercategory `Â` of (5.2) must carry a minus sign, `ĝ ∘ f̂ := -ξ_ν ∘ Πg ∘ f`. The printed proof uses `ζ_{Πν} ∘ Πg = -g ∘ ζ_μ` for the *even* morphism `g : μ → Πν`; supernaturality of `ζ` gives `ζ_{Πν} ∘ Πg = g ∘ ζ_μ`. With the printed rule `T_A` reverses the sign of composites of odd morphisms and `E₁ ∘ D₁` replaces `ξ` by `-ξ` (for `A = SVec` the printed rule gives `ĝ ∘ f̂ = -(g ∘ f)` on odd linear maps). Since `(A, Π, ξ) ↦ (A, Π, -ξ)` is an automorphism of `Π-Cat`, the statement of Theorem 1.9 is unaffected. `StringDiagrams.Super.Associated` uses the corrected sign.

**Errata (§5, (5.5) and the proof of Lemma 5.4).** The same sign error propagates to the associated Π-2-supercategory `𝔄̂` of a Π-2-category `(𝔄, π, β, ξ)`:

1. *Vertical composition in (5.5) and in the proof of Lemma 5.4.* Printed: for odd `x̂ : F ⇒ G`, `ŷ : G ⇒ H` coming from `x : F ⇒ π_μ G`, `y : G ⇒ π_μ H`, `ŷ ∘ x̂ := ξ_μ H ∘ π_μ y ∘ x`. Corrected: `ŷ ∘ x̂ := -ξ_μ H ∘ π_μ y ∘ x` (`Associated2.comp₂_fst`). Reason: the proof of Lemma 5.4 uses `y ∘ ζ_μ G = -ζ_μ π_μ H ∘ π_μ y` for the *even* 2-morphism `y`; supernaturality of `ζ_μ -` gives `y ∘ ζ_μ G = ζ_μ π_μ H ∘ π_μ y`.
2. *Horizontal composition of two odd 2-morphisms in (5.5).* Printed: for odd `x̂ : F ⇒ H`, `ŷ : G ⇒ K` coming from `x : F ⇒ π_μ H`, `y : G ⇒ π_ν K`, `ŷx̂ := -ξ_ν KH ∘ π_ν (β_{ν,μ})⁻¹_K H ∘ yx`. Corrected: `ŷx̂ := ξ_ν KH ∘ π_ν (β_{ν,μ})⁻¹_K H ∘ yx` (`Associated2.hcomp_odd_odd`; in the one-object case of Theorem 1.15, `f̂ ⊗ ĝ = ξ ∘ (1_π ⊗ β⁻¹ ⊗ 1) ∘ (f ⊗ g)`, `Associated.superTensorHom_odd_odd`). Reason: the proof of Lemma 5.4 asserts `-ζ_ν K ζ_μ H ∘ yx = (ζ_ν K ∘ y)(ζ_μ H ∘ x)`, whereas the super interchange law gives `(ζ_ν K ∘ y)(ζ_μ H ∘ x) = (ζ_ν K ζ_μ H) ∘ (yx)` because `y` is even. With the printed sign, `ζ̂ζ̂ = -ξ` in `𝔄̂`, so `E₂ ∘ D₂ ≠ I`.

The printed signs in 1 and 2 are those of the construction applied to `(𝔄, π, β, -ξ)`, which is again a Π-2-category, and `(π, β, ξ) ↦ (π, β, -ξ)` (the identity on Π-2-functors: both sides of the second axiom of Definition 5.2(ii) change sign) is an automorphism of `Π-2-Cat`, and likewise of `Π-Mon`. Hence the statements of Lemma 5.4, Theorem 5.5, Corollary 5.6 and Theorem 1.15 remain true as printed; only the explicit construction of `D₂` (and of the inverse `Π-Mon → Π-SMon`) and the identities `E₂ ∘ D₂ = I` and `𝕋` in the proofs need the corrected signs, which `StringDiagrams.Super.AssociatedTwo` and `StringDiagrams.Super.MonoidalAssociated` use.

`StringDiagrams.Examples.PiEnvelope`: the Π-envelope of the odd Brauer supercategory; the odd cup and cap become even morphisms `Π⁰(n) → Π¹(n+2)` and `Π¹(n+2) → Π⁰(n)` satisfying the zigzag relation.

### Superspaces, superalgebras and super Karoubi envelopes

Following Brundan–Ellis, *Monoidal supercategories* ([arXiv:1603.05928v3](https://arxiv.org/abs/1603.05928)), Examples 1.2, 1.5, 1.8, 1.13, §1.5 and Example 1.17, over a commutative ground ring `k` (as in §2 of the paper).

| Module | Contents |
| --- | --- |
| `StringDiagrams.Super.SVec` | Superspaces `SVec k` (a `k`-module with the projection onto its odd part; homogeneous components `part p`, `isInternal_part`; constructions `ofProd`, `ofIsCompl`) with all linear maps as morphisms. Example 1.2(i): `SVec k` is a supercategory (`instSupercategory`, parity components `homProj`). Example 1.5(i): a monoidal supercategory (`instMonoidalSupercategory`) with the Koszul sign rule `(1 ⊗ g)(v ⊗ w) = (-1)^{\|g\|\|v\|} v ⊗ g(w)` (`whiskerLeft_tmul`) and `(f ⊗ g)(v ⊗ w) = (-1)^{\|g\|\|v\|} f(v) ⊗ g(w)` for the paper's tensor product (`superTensorHom_tmul`). Example 1.8 (`A = B = k`): a Π-supercategory with `ζ_V` the identity function (`instPiSupercategory`), `Π f = (-1)^{\|f\|} f` (`pi_map_of_mem`), `ξ_V = -1` (`ξ_hom`). Example 1.13(i) (`A = k`): a monoidal Π-supercategory with `π = Π k` (`instMonoidalPiSupercategory`). |
| `StringDiagrams.Super.K0` | §1.5: the split Grothendieck group `K₀ C` of a preadditive category with binary biproducts (generated by classes `[V]`, `[V] = [W]` for `V ≅ W`, `[V ⊞ W] = [V] + [W]`: `mk_eq_mk_of_iso`, `mk_biprod`), its universal property (`lift`, `lift_mk`, `hom_ext`), every element is `[V] - [W]` (`exists_eq_mk_sub_mk`), functoriality in additive functors (`map`, invariant under isomorphism of functors: `map_eq_of_iso`); the ring `Zπ = ℤ[π]/(π² − 1)` (`Zπ`, `Zπ.π_mul_π`) and the `Zπ`-module structure given by an involution (`Zπ.moduleOfInvolution`); for a Π-category, `π [V] = [Π V]` is an involution of `K₀` (`K₀.piInvolution_piInvolution`), making `K₀` a `Zπ`-module (`K₀.moduleZπ`, `K₀.π_smul_mk`). |
| `StringDiagrams.Super.SKar` | §1.5: the additive envelope `Mat_` and the idempotent completion `Karoubi` of an `R`-linear category are `R`-linear, and a Π-category structure extends to both (`Mat_.instPiCategory`, `Karoubi.instPiCategory`). The super Karoubi envelope `SKar R A := Kar(A̲_π)` (the additive Karoubi envelope of the underlying category of the Π-envelope) is an additive, idempotent complete, `R`-linear Π-category (`SKar.instPiCategory`) containing `A̲_π` (`SKar.of`, full and faithful); `K₀(SKar(A))` is a `Zπ`-module (`SKar.moduleZπ`, `SKar.π_smul_mk`). |
| `StringDiagrams.Super.KaroubiMonoidal` | The additive envelope `Mat_ D` of a monoidal preadditive category is monoidal (entrywise tensor product of matrices, coherence isomorphisms given by permutation matrices: `Mat_.instMonoidalCategory`, `Mat_.instMonoidalPreadditive`), and the idempotent completion of a monoidal category is monoidal with `(X, p) ⊗ (Y, q) = (X ⊗ Y, p ⊗ q)` (`Karoubi.instMonoidalCategory`, `Karoubi.instMonoidalPreadditive`). |
| `StringDiagrams.Super.SKarMonoidal` | §1.5 for a monoidal supercategory `A`: `SKar(A)` is a monoidal additive category; in `A̲_π`, `Π ≅ π ⊗ - ≅ - ⊗ π` (`Underlying.piIsoTensorLeft`, `Underlying.piIsoTensorRight`), hence `Π V ≅ π ⊗ V ≅ V ⊗ π` in `SKar(A)` (`SKar.piObjIsoTensorLeft`, `SKar.piObjIsoTensorRight`); `K₀(SKar(A))` is a ring (`K₀.instRing`, `[V][W] = [V ⊗ W]`, `K₀.mk_mul_mk`) and a `Zπ`-algebra with `π ↦ [π]` (`SKar.instAlgebraZπ`; `[π]² = 1`: `SKar.piObj_mul_piObj`; `[π]` central: `SKar.piObj_commute`), whose `π`-action is `[V] ↦ [Π V]` (`SKar.π_smul_eq`). |
| `StringDiagrams.Super.SKarMonoidalPi` | §1.5: a monoidal Π-category structure (Definition 1.14) extends to the additive envelope and to the idempotent completion (`Mat_.instMonoidalPiCategory`, `Karoubi.instMonoidalPiCategory`; half-braidings `Mat_.halfBraiding`, `Karoubi.halfBraiding`); hence for a monoidal supercategory `A`, `SKar(A)` is a monoidal Π-category (`SKar.instMonoidalPiCategory`). |
| `StringDiagrams.Super.EndMonoidal` | Example 1.5(ii): `End(A)` (superfunctors `A → A`, `SuperEnd R A`) is a strict monoidal supercategory with `F ⊗ G = F ∘ G` and `(x ⊗ y)_λ = x_{Kλ} ∘ F y_λ` (`SuperEnd.instMonoidalSupercategory`, `SuperEnd.instIsStrict`, `SuperEnd.superTensorHom_eq`). Example 1.13(ii): for a Π-supercategory `(A, Π, ζ)`, `(End(A), Π, ζ)` is a monoidal Π-supercategory (`SuperEnd.instMonoidalPiSupercategory`). |
| `StringDiagrams.Super.SKarUnit` | Example 1.17(ii): the functor `SKar(I) ⥤ SVec̲`, `(M, p) ↦ p(⊕ᵢ Π^{aᵢ} k)` (`SKarUnit.toSVec`), is additive, full and faithful, and over a field every finite-dimensional superspace is evenly isomorphic to an object in its image (`SKarUnit.exists_iso_toSVec_obj`): `SKar(I) ≃ SVec_fd`. Objects are classified by the dimensions of their even and odd parts (`SKarUnit.nonempty_iso_of_dim_eq`), and `K₀(SKar(I)) ≅ Zπ` as `Zπ`-algebras with inverse `[V] ↦ dim V₀ + (dim V₁) π` (`SKarUnit.K₀AlgEquiv`, `SKarUnit.K₀AlgEquiv_symm_mk`). The monoidality of the equivalence and semisimplicity are not formalized. |
| `StringDiagrams.Super.SKarSuperalgebra` | Example 1.17(i): for a superalgebra `A`, the functor `SKar(A) ⥤ (k-SMod-A)̲` to right `A`-supermodules and even homomorphisms, `(M, p) ↦ p(⊕ᵢ Π^{aᵢ} A)` (`SKarAlg.toMod`), is additive, full and faithful (`SKarAlg.toMod_full`, `SKarAlg.toMod_faithful`), and its essential image is exactly the finitely generated projective supermodules, i.e. even direct summands of finite free supermodules (`SKarAlg.IsFGProjective`, `SKarAlg.isFGProjective_toMod_obj`, `SKarAlg.exists_iso_toMod_obj`). The identification of `K₀(SKar(A))` with the split Grothendieck group of `A` is not formalized. |
| `StringDiagrams.Super.Superalgebra` | Example 1.2(ii): a superalgebra (a `k`-algebra with a `GradedAlgebra` grading by `ZMod 2`) is a supercategory with one object (`SuperalgebraCat`, `SuperalgebraCat.instSupercategory`). The unit supercategory `I` (one object, endomorphisms `k` in even parity: `UnitSupercat k`) is a strict monoidal supercategory (`UnitSupercat.instMonoidalSupercategory`, `instIsStrict`), and this strict monoidal structure is unique (`UnitSupercat.monoidalStruct_eq`) (Example 1.17(ii)); the canonical superfunctor `I → SVec` (`UnitSupercat.toSVec`) is monoidal (`UnitSupercat.toSVecMonoidal`). |
| `StringDiagrams.Super.Superbimodule` | Example 1.2(iii): `(A, B)`-superbimodules for superalgebras `A`, `B` (`SuperBimodule`: a superspace with even left and right actions) and superbimodule homomorphisms (`SuperBimodule.IsHom`: `f(avb) = (-1)^{\|f\|\|a\|} a f(v) b`, `isHom_iff_of_mem`) form a supercategory (`SuperBimodule.instSupercategory`; parity components of homomorphisms are homomorphisms, `SuperBimodule.IsHom.proj`). Example 1.8: it is a Π-supercategory with `Π V` the opposite grading and `a · v · b = (-1)^{\|a\|} a v b` (`piObj`, `piObj_lact_of_mem`), `ζ_V` the identity function, `Π f = (-1)^{\|f\|} f` (`pi_map_of_mem`), `ξ_V = -1` (`ξ_hom`). The monoidal structure on `A-SMod-A` (Examples 1.5(i), 1.13(i)) is formalized only for `A = k` (`SVec`): Mathlib has no balanced tensor product over a noncommutative ring. |

### Graded supercategories and (Q, Π)-structures

Following Brundan–Ellis, *Monoidal supercategories* ([arXiv:1603.05928v3](https://arxiv.org/abs/1603.05928)), §6. Compositions are written in diagrammatic order; the degree conventions are those of Definition 6.4 (`σ : Q ⇒ I` has degree `-1`).

| Module | Contents |
| --- | --- |
| `StringDiagrams.Super.Graded` | Definition 6.1: graded supercategories (`GradedSupercategory R C`, `GSVec`-enrichment unpacked on top of `Supercategory`: degree submodules `degree X Y n`, internal direct sum, stable under the parity projections; each morphism space is the bigraded module `⨁_{(n,p)} V_{n,p}`: `GradedSupercategory.isInternal_bigrading`), degree projections (`GradedSupercategory.dproj`), graded superfunctors (`GradedSupercategory.IsGradedSuperfunctor`), graded supernatural transformations (`GradedSupercategory.IsGradedSupernatural`, homogeneous of parity `p` and degree `n`; finite sums: `GradedSupercategory.GradedSuperNatTrans`), graded superequivalences (`GradedSupercategory.GradedSuperequivalence`, `GradedSupercategory.GradedSuperequivalence.ofFullyFaithful`). The underlying category (even morphisms of degree zero) is `GradedSupercategory.GUnderlying R C`, via the supercategory `GradedSupercategory.DegreeZero R C` of degree-zero morphisms. |
| `StringDiagrams.Super.QPi` | Definition 6.4: graded `(Q, Π)`-supercategories (`QPiSupercategory R C`, extending `PiSupercategory`; `QPiSupercategory.ofIso`); `Π`, `Q`, `Q⁻¹` are graded (`QPiSupercategory.pi_isGraded`, `QPiSupercategory.Q_isGraded`, `QPiSupercategory.Qinv_isGraded`). Corollary 6.7: (i) `ξ`, `ii = σ̄σ`, `jj = σσ̄` even of degree zero, `Q ii = jj Q`, `ii Q⁻¹ = Q⁻¹ jj` (`QPiSupercategory.ξ_hom_mem_degree`, `QPiSupercategory.ii_hom_mem_degree`, `QPiSupercategory.jj_hom_mem_degree`, `QPiSupercategory.Q_map_ii`, `QPiSupercategory.ii_Qinv`, `QPiSupercategory.qEquivalence`); (ii) `β_F`, `γ_F := σ' F σ⁻¹` even of degree zero, `γ_Π = β_Q⁻¹` (`QPiSupercategory.β_hom_mem_degree`, `QPiSupercategory.γ`, `QPiSupercategory.γ_hom_mem_degree`, `QPiSupercategory.γ_naturality`, `QPiSupercategory.γ_pi`); (iii) `QPiSupercategory.γ_naturality_supernatural` (erratum 1 below); (iv) `QPiSupercategory.γ_comp`, `QPiSupercategory.γ_id`, `QPiSupercategory.γ_Q`. The compatibility of `γ_F` with `β` used in Theorem 6.13: `QPiSupercategory.β_γ_compat`. Powers `Q^m` and `σ^m : Q^m ≅ I` of degree `-m`: `QPiSupercategory.qPow`, `QPiSupercategory.σPow`. |
| `StringDiagrams.Super.QEnvelope` | Definition 6.8: the `(Q, Π)`-envelope `QPiEnvelope R A := Envelope R (QEnvelope R A)` (objects `Q^m Π^a λ`; morphisms `QPiEnvelope.ofHom`, of parity and degree `QPiEnvelope.ofHom_mem`, `QPiEnvelope.ofHom_mem_degree`), a graded `(Q, Π)`-supercategory (`QPiEnvelope.instQPi`), with `QPiEnvelope.J`. The analogue of Lemma 4.1: `QPiEnvelope.QPiComplete`, `QPiEnvelope.J_gradedEvenlyDense_iff`, `QPiEnvelope.gradedSuperequivalenceJ`, `QPiEnvelope.qpiComplete_of_gradedSuperequivalence`. The universal property: `QPiEnvelope.extend`, `QPiEnvelope.J_comp_extend`, `QPiEnvelope.extendNat`, `QPiEnvelope.isGradedSupernatural_extendNat`, `QPiEnvelope.eq_extendNat`. Theorem 6.9: restriction along `J` is bijective on supernatural transformations homogeneous of each parity and degree (`QPiEnvelope.restrict_bijective`), extension respects identities and composition (`QPiEnvelope.extendNat_id`, `QPiEnvelope.extendNat_comp`), and every graded superfunctor out of `A_{q,π}` is evenly isomorphic in degree zero to the extension of its restriction (`QPiEnvelope.extendRestrictIso`, `QPiEnvelope.extendRestrictIso_hom_mem_degree`). `-_{q,π}` on graded superfunctors: `QPiEnvelope.map`, `QPiEnvelope.J_comp_map`, `QPiEnvelope.map_id`, `QPiEnvelope.map_comp`. The 2-adjunction packaging of Theorem 6.9 is not formalized. |
| `StringDiagrams.Super.QPiCategory` | Definition 6.12: `(Q, Π)`-categories (`QPiCategory`, extending `PiCategory`; `QPiCategory.qEquivalence`), `(Q, Π)`-functors (`QPiFunctor`, with `QPiFunctor.id`, `QPiFunctor.pi`, `QPiFunctor.Q`, `QPiFunctor.comp`) and `(Q, Π)`-natural transformations (`QPiFunctor.IsQPiNatural`, `QPiFunctor.isQPiNatural_id`, `QPiFunctor.IsQPiNatural.comp`); the compatibility condition `QPiFunctor.IsCompatible` (erratum 4 below), invariant under `(Q, Π)`-natural isomorphism (`QPiFunctor.IsCompatible.of_iso`). (6.3), the strict 2-functor `𝔼`: `GradedSupercategory.GUnderlying.instQPiCategory`, `GradedSupercategory.GUnderlying.qpiFunctor` (always compatible: `GradedSupercategory.GUnderlying.qpiFunctor_isCompatible`), `GradedSupercategory.GUnderlying.natTrans`, `GradedSupercategory.GUnderlying.isQPiNatural`, `GradedSupercategory.GUnderlying.map_comp`, `GradedSupercategory.GUnderlying.map_id`, `GradedSupercategory.GUnderlying.qpiFunctor_comp_β`, `GradedSupercategory.GUnderlying.qpiFunctor_comp_γ`. |
| `StringDiagrams.Super.Orbit` | The orbit supercategory `Orbit d` of a shift datum (`ShiftData`: an adjoint auto-equivalence by superfunctors with even unit and counit): morphisms of degree `m` are families `(f_{i,j} : Qⁱλ → Qʲμ)_{i-j=m}` compatible with `Q` (`ShiftData.Fam`), so composition is strictly associative without the coherence isomorphisms `QᵐQⁿ ≅ Qᵐ⁺ⁿ`. It is a graded supercategory, and a graded `(Q, Π)`-supercategory when `S` is a Π-supercategory (`Orbit.instQPiSupercategory`); functoriality in morphisms of shift data (`Orbit.map`, `Orbit.ι_comp_map`) and in compatible natural transformations (`Orbit.mapNat`, `Orbit.isGradedSupernatural_mapNat`). |
| `StringDiagrams.Super.QAssociated` | Theorem 6.13, the 2-functor `𝔻` and `𝔻 ∘ 𝔼 ≅ I`, `𝔼 ∘ 𝔻 = I`. Objects: the graded `(Q, Π)`-supercategory `QAssociated R A` associated to a `(Q, Π)`-category (the orbit supercategory of `Q̂` on the associated Π-supercategory `Associated R A` of §5, `QAssociated.shiftData`; erratum 3 below); `QAssociated.unit`, `QAssociated.counit` (`QAssociated.unit_comp_counit`, `QAssociated.counit_comp_unit`) commute with `Π`, `Q`, `ξ` (`QAssociated.unit_map_pi`, `QAssociated.unit_map_Q`, `QAssociated.ξ_unit`, `QAssociated.unitQPiFunctor`). 1-morphisms: `QAssociated.map` on compatible `(Q, Π)`-functors, `QAssociated.unit_comp_map`. 2-morphisms: `QAssociated.mapNat`, `QAssociated.isGradedSupernatural_mapNat`, `QAssociated.mapNat_id`, `QAssociated.mapNat_comp`, `QAssociated.unit_map_app_mapNat`. `T_B : 𝔻(𝔼 B) ⥤ B`: `QAssociated.T`, an isomorphism (`QAssociated.T_comp_Tinv`, `QAssociated.Tinv_comp_T`) carrying `ζ`, `σ` to `ζ`, `σ` (`QAssociated.T_map_ζ`, `QAssociated.T_map_σ`), natural in 1-morphisms (`QAssociated.T_naturality`) and 2-morphisms (`QAssociated.T_map_mapNat_app`). Horizontal composition of 2-morphisms and the packaging as strict 2-functors are not formalized. |
| `StringDiagrams.Super.GradedTwo` | Definition 6.2: graded 2-supercategories (`GradedTwoSupercategory`, `GradedTwoSupercategory.hcomp_mem_degree`); Definition 6.3: graded 2-superfunctors (`TwoSuperfunctor.IsGraded`, `TwoSuperfunctor.id_isGraded`), graded 2-natural transformations and supermodifications as predicates (`TwoNatTrans.IsGraded`, `Supermodification.IsHomogeneous`). Definition 6.5: graded `(Q, Π)`-2-supercategories (`QPiTwoSupercategory`, extending `PiTwoSupercategory`). Lemma 6.6, for not necessarily strict 2-supercategories: `QPiTwoSupercategory.β_hom_mem_degree`, `QPiTwoSupercategory.ξ_hom_mem_degree`, `QPiTwoSupercategory.γ` (`QPiTwoSupercategory.γ_hom_mem_degree`, `QPiTwoSupercategory.γ_naturality`, `QPiTwoSupercategory.γ_comp`, `QPiTwoSupercategory.γ_id`, `QPiTwoSupercategory.γ_q`, `QPiTwoSupercategory.γ_pi`; `(q, γ)` in the Drinfeld center: `QPiTwoSupercategory.qCenterObj`, `QPiTwoSupercategory.centerσ`), `QPiTwoSupercategory.ii`, `QPiTwoSupercategory.jj` (`QPiTwoSupercategory.ii_hom_mem_degree`, `QPiTwoSupercategory.jj_hom_mem_degree`, `QPiTwoSupercategory.q_ii`, `QPiTwoSupercategory.ii_qinv`). |
| `StringDiagrams.Super.GradedTwoEnvelope` | Definition 6.10: the `(Q, Π)`-envelope `QPiTwoEnvelope R B := TwoEnvelope R (QTwoEnvelope R B)` of a graded 2-supercategory, whose morphism supercategories are the `QPiEnvelope`s; a graded 2-supercategory (`QTwoEnvelope.gradedTwoSupercategory`, `TwoEnvelope.gradedTwoSupercategory`) and a graded `(Q, Π)`-2-supercategory (`QPiTwoEnvelope.instQPiTwoSupercategory`); the horizontal composition sign rule `(-1)^{b\|x\| + \|y\|c + bc + ab}` (`QPiTwoEnvelope.toHom_hcomp`); `(Q, Π)`-completeness (`QPiTwoEnvelope.QPiComplete2`, `QPiTwoEnvelope.qpiComplete2_iff`, `QPiTwoEnvelope.J_gradedEvenlyDense_iff`). |
| `StringDiagrams.Super.QPiTwoCategory` | Definition 6.14: `(Q, Π)`-2-categories (`QPiTwoCategory`, extending `PiTwoCategory`); the underlying 2-category `GUnderlying2 R B` of a graded 2-supercategory (via `DegreeZero2 R B`) is a `(Q, Π)`-2-category when `B` is a graded `(Q, Π)`-2-supercategory (`GUnderlying2.instQPiTwoCategory`). |
| `StringDiagrams.Super.GSKar` | End of §6: a `(Q, Π)`-category structure extends to `Mat_` and `Karoubi` (`Mat_.instQPiCategory`, `Karoubi.instQPiCategory`); the graded super Karoubi envelope `GSKar R A := Kar(A̲_{q,π})` is an additive, idempotent complete `(Q, Π)`-category (`GSKar.instQPiCategory`), and `K₀(GSKar(A))` is a `Zπ[q, q⁻¹]`-module (`GSKar.moduleLaurent`) with `π [V] = [Π V]`, `q [V] = [Q V]`, `q⁻¹ [V] = [Q⁻¹ V]` (`K₀.C_π_smul_mk`, `K₀.T_smul_mk`, `K₀.T_neg_one_smul_mk`). |

`StringDiagrams.Examples.QPiCompat`: over a ring with `2 ≠ 0`, a `(Q, Π)`-functor in the sense of the printed Definition 6.12(ii) which is not compatible (`QPiCompat.twisted`, `QPiCompat.twisted_not_isCompatible`), hence not isomorphic to `𝔼(F)` for any graded superfunctor `F` (`QPiCompat.twisted_not_iso_image`); see erratum 4.

Not formalized from §6: Lemma 6.11 (extension of graded 2-superfunctors and graded 2-natural transformations to `(Q, Π)`-envelopes of 2-supercategories) and the 2-adjunction packaging after Definition 6.10; the associated graded `(Q, Π)`-2-supercategory of a `(Q, Π)`-2-category and the analogue of Theorem 5.5 (no 2-categorical orbit construction yet); the 2-categorical graded super Karoubi envelope `GSKAR(𝔄)` and its Grothendieck ring with idempotents `1_λ`; the examples `GSVec`, `GSMod`, `GSCAT`, `(Q,Π)-GSCAT`, `𝔊𝔖𝔅𝔦𝔪`.

**Errata (§6).**

1. *Corollary 6.7(iii).* Printed: `γ_G ∘ Q'x = xQ ∘ γ_G`. Corrected: `γ_G ∘ Q'x = xQ ∘ γ_F` (`QPiSupercategory.γ_naturality_supernatural`); the printed right-hand side is not a composable pair unless `F = G`.
2. *Proof of Corollary 6.7(i).* The adjunction composite is printed as `Q ii⁻¹ ∘ jj Q : Q ⇒ QQ⁻¹Q ⇒ Q`, and similarly for `Q⁻¹`; the composite `Q ⇒ QQ⁻¹Q ⇒ Q` is `jj Q ∘ Q ii⁻¹` (order of composition). The statement is unaffected (`QPiSupercategory.Q_map_ii`, `QPiSupercategory.ii_Qinv`, `QPiSupercategory.qAdjunction`).
3. *Proof of Theorem 6.13, the associated graded `(Q, Π)`-supercategory.* With the degree conventions of Definition 6.4 (`σ : Q ⇒ I` of degree `-1`), the morphisms `λ → μ` of degree `m` and parity `a` must be `Hom_A(λ, Q^{-m} Π^a μ)`, not `Hom_A(λ, Q^m Π^a μ)` as printed: the isomorphism `σ_λ : Qλ → λ` comes from `1_{Qλ}` and must have degree `-1`. `QAssociated` uses the corrected convention. Its Π-part is the associated Π-supercategory of §5, with the corrected sign of the §5 erratum (the composite of two odd morphisms carries `-ξ`).
4. *Definition 6.12(ii) and Theorem 6.13.* The construction of `𝔻` on 1-morphisms needs `γ_F` to be compatible with `β`: `γ_F Π ∘ Q'β_F ∘ β_{Q'} F = F β_Q ∘ β_F Q ∘ Π'γ_F` (`QPiFunctor.IsCompatible`). This is not among the axioms of Definition 6.12(ii). It holds for every `𝔼(F)` (`GradedSupercategory.GUnderlying.qpiFunctor_isCompatible`, from `QPiSupercategory.β_γ_compat`) and is invariant under `(Q, Π)`-natural isomorphism (`QPiFunctor.IsCompatible.of_iso`), so with the printed definition `𝔼` is not essentially surjective on 1-morphisms and Theorem 6.13 fails (`QPiCompat.twisted_not_iso_image`). With the compatibility added to Definition 6.12(ii), the constructions above give `𝔻` on objects, 1-morphisms and 2-morphisms, `𝔼 ∘ 𝔻 = I` and `T : 𝔻 ∘ 𝔼 ≅ I`.

### Example: the walking biadjunction

`StringDiagrams.Examples.WalkingBiadjunction`: strands `E : a → b`, `F : b → a` with all four cups and caps and a dot on `E` whose two rotations agree; every 2-morphism is cyclic (`pivotal`), the dot slides along both cups, and the plain bubble equals the trace of `snakeDown ≫ snakeUp` (`bubble_eq_trace_snakes`), a consequence of trace cyclicity.

### Example: a cyclic generator with a two-letter boundary

`StringDiagrams.Examples.TwoStrandGenerator`: strands `E : a → b`, `F : b → a` with `E* = F` and a generator `s : E F ⟶ E F`, presented as the cyclic pivotal extension (the two rotations of `s` by the nested cups and caps of `E F` are identified); the presented bicategory is pivotal in one line (`pivotal`). Derived identities of explicit diagrams: the zigzag identity for the nested cups and caps of `E F` (`zigzagEF_eq_id`), and `s` slides along the nested cups of `E F` from either copy of `E F` to the other, becoming its rotation (`s_slide_cup`, `s_slide_cup'`, the latter using cyclicity).

### Example: odd dots

`StringDiagrams.Examples.Exterior`: a single odd dot with `dot ≫ dot = 0`; distinct dots anticommute by the super interchange law alone (`x_mul_x_anticomm`).

### Example: free commuting dots

`StringDiagrams.Examples.FreeDots`: a single even dot and no relations. Dots on distinct strands commute by the interchange law alone (`x_mul_x_comm`, for symbolic width and positions); the presented category is strict monoidal (`FD.obj_tensor`, `x_whiskerRight`, `whiskerLeft_x`, `x_whiskerRight_strands`).

| Module | Contents |
| --- | --- |
| `StringDiagrams.Examples.FreeDots.Representation` | The representation on `MvPolynomial ℕ R` with a dot on strand `i` acting by multiplication by `X i` (`polyRep`, `polyRep_map_x`), as a uniform interpretation by local operators; non-vacuity over a nontrivial ring: `x_ne_zero`, and `x_ne_x` (dots on distinct strands are distinct). |

### Example: bubbles in weighted regions

`StringDiagrams.Examples.WeightedBubbles`: a small test of the region-aware part of the library, loosely modelled on the `sl₂` case of Khovanov–Lauda's 2-category ([arXiv:0807.3250](https://arxiv.org/abs/0807.3250)) but not a formalization of it. Regions are weights in `ℤ`, strands `E`, `F` change the weight by `2`, and a bubble in the region `λ` evaluates to `c λ` for a given `c : ℤ → R`. Two bubbles on either side of a strand drawn at interleaved heights evaluate to `c (μ + 4) c (μ + 2)` (`interleaved`, via the interchange law for general regions); in the presented bicategory, `bubble ▷ E` and `E ◁ bubble` are distinguished by a scalar representation whenever the two regions have different values (`leftBubble_ne_rightBubble`).

### Example: Temperley–Lieb

`StringDiagrams.Examples.TemperleyLieb`: one self-dual colour with a cup and a cap, the two zigzag relations and the loop relation `cap ∘ cup = δ`. For every width, `e_i = cup_i ∘ cap_i` satisfies `e_i² = δ e_i` and `e_i e_{i±1} e_i = e_i` (`e_mul_self`, `e_mul_e_succ_mul_e`, `e_succ_mul_e_mul_e_succ`).

| Module | Contents |
| --- | --- |
| `StringDiagrams.Examples.TemperleyLieb.FarCommutativity` | Interchange of cups and caps at symbolic positions and widths (`cap_cap_interchange`, `cup_cup_interchange`, `cap_cup_interchange`, `cup_cap_interchange`); far commutativity `e_i e_j = e_j e_i` for `i + 2 ≤ j` in every width (`e_mul_e_comm`, `e_mul_e_comm_of_far`). |
| `StringDiagrams.Examples.TemperleyLieb.Representation` | The representation of `pres R (-2)` on `(R²)^{⊗n}` over any commutative ring (`Rep.rep`), with cup `e₀ ⊗ e₁ − e₁ ⊗ e₀` and the inverse pairing as cap; soundness via `Presentation.lift` (`Rep.respects`); non-vacuity `e (-2) m i ≠ 0` for `i ≤ m` over a nontrivial ring (`Rep.e_ne_zero`). |

### Example: the odd Brauer supercategory

`StringDiagrams.Examples.OddBrauer`: Brundan–Ellis, Example 1.5(iii) (the marked Brauer category of Kujawa–Tharp): an even crossing and odd cup and cap with the relations of the paper (`s² = 1`, braid, zigzags `1` and `-1`, a strand sliding through a cup, `cup ≫ s = cup`), a strict monoidal supercategory (`OddBrauer.monoidalSupercategory`). Using the relations and the super interchange law: `s ≫ cap = -cap` (`cross_comp_cap`), hence the bubble is minus itself, `2 • bubble = 0` over any commutative ring (`two_smul_bubble`), and `bubble = 0` when `2` is invertible (`bubble_eq_zero`).

### Example: the odd Temperley–Lieb supercategory

Brundan–Ellis, *Monoidal supercategories* ([arXiv:1603.05928v3](https://arxiv.org/abs/1603.05928)), Example 1.17(iii) and Appendix A (with `ε = -1`). `STL(δ)` has one generating object and odd generators cup and cap subject to `cup₁ ≫ cap₀ = 1`, `cup₀ ≫ cap₁ = -1` and `cup ≫ cap = δ`.

| Module | Contents |
| --- | --- |
| `StringDiagrams.Examples.OddTemperleyLieb.Basic` | The presentation (`OddTemperleyLieb.pres`), parity-homogeneous; `STL(δ)` is a strict monoidal supercategory (`OddTemperleyLieb.monoidalSupercategory`); `n = X n` is the `n`-fold tensor power (`X_tensor`), `e_n = 𝟙 (X n)`; cups and caps at symbolic positions (`cup`, `cap`), the relations at every position (`zigzagA_at`, `zigzagB_at`, `loop_at`) and the super interchange law for generators (`cup_cup`, `cap_cap`, `cup_cap`, `cap_cup`). |
| `StringDiagrams.Examples.OddTemperleyLieb.Representation` | Lemma A.1: the representation `G` on `V^{⊗n}` (`Rep.rep q`, `δ = -(q - q⁻¹)`, any commutative ring and unit `q`), modelled on functions on words with the Koszul signs of `1_l ⊗ g ⊗ 1_r` (`Rep.psign`, `Rep.cupOp`, `Rep.capOp`); soundness (`Rep.respects`); the values `G(cup)(1) = v₋₁ ⊗ v₁ - q v₁ ⊗ v₋₁` (`Rep.rep_cup_vec`) and `G(cap)(v_a ⊗ v_b) = β(a, b)` with `β(v₋₁, v₁) = -ε q⁻¹` (`Rep.rep_cap_vec`). |
| `StringDiagrams.Examples.OddTemperleyLieb.MonoidalRep` | Lemma A.1 as stated: `G : STL(δ) ⥤ SVec` with `G(n) = V^{⊗n}` (`Rep.repS`, `Rep.sv`) is a superfunctor (`Rep.instIsSuperfunctor`) and a monoidal superfunctor (`Rep.repSMonoidal`), with coherence isomorphisms the concatenations `V^{⊗m} ⊗ V^{⊗n} ≅ V^{⊗(m+n)}` (`Rep.catE`) and `k ≅ V^{⊗0}`; naturality in the second variable is the Koszul sign rule for left whiskering (`Rep.opList_wl_catF`). |
| `StringDiagrams.Examples.OddTemperleyLieb.QuantumIntegers` | `[n]_{x,π}` (`qbracket`) with (1.10) (`qbracket_succ_eq`), (1.11) (`qbracket_mul`), and (1.12) in corrected form (`qbracket_genFun_mul`, see the erratum below); `[n] = [n]_{q,ε}` (`qint`) with `(q - εq⁻¹)[n] = qⁿ - (εq)⁻ⁿ` (`qint_mul_eq`), `[n][2] = [n+1] + ε[n-1]` (`qint_mul_two`), `[n] ≠ 0` for `q` not a root of unity (`qint_ne_zero`). |
| `StringDiagrams.Examples.OddTemperleyLieb.Dyck`, `…Words`, `…Canonical` | Dyck sequences (`IsDyck`) as crossingless matchings; following arcs through cups and caps (`capT`, `loopT`, `trackW`); words in cups and caps (`evW`, `diag_eq_evW`), the snake identities (`evW_snake`, `evW_snake'`); canonical cup diagrams (`canon`), `canon ≫ evW u = ± δ^ℓ canon (trackW …)` (`canon_comp_evW`), spanning of `Hom(0, N)` (`span_canon`). |
| `StringDiagrams.Examples.OddTemperleyLieb.Independence`, `…Basis` | Theorem A.2: triangularity of `G` on canonical cup diagrams for the dominance order of Dyck sequences (`canonVec_triangular`); crossingless matchings `m → n` (`CrossinglessMatching`), the crossingless matching and loop number of a diagram (`matchingOf`, `loopsOf`), isotopy invariance up to sign (`bend_diag`); any set of representatives is a basis of `Hom(m, n)` (`linearIndependent_and_span_of_representatives`, `basisOfRepresentatives`, `theoremA2`), representatives exist (`exists_representatives`); `dim Hom(3, 3) = 5` (`finrank_hom_three_three`). Proved for any commutative ring and unit `q`. |
| `StringDiagrams.Examples.OddTemperleyLieb.JonesWenzl`, `…HomSpaces`, `…Decomposition` | Super Jones–Wenzl projectors (`jw`): even, idempotent (`jw_idem`), killing caps on top and cups below (`jw_comp_cap`, `cup_comp_jw`), the partial trace `-([n+2]/[n+1]) f_n` (`jw_partial_trace`), `f_n ≠ 0` (`jw_ne_zero`); every morphism is a combination of caps followed by cups (`mem_nfSpan`), so `f_m A f_n = 0` for `m ≠ n` and `f_n A f_n = k f_n` (`jw_comp_comp_jw_of_ne`, `jw_comp_comp_jw_self`); `f_{n-1} ⊗ 1 = f_n + g_n` with `v_n ≫ u_n = g_n`, `u_n ≫ v_n = f_{n-2}` for odd `u_n`, `v_n` (`wR1_jw_eq`, `vDec_comp_uDec`, `uDec_comp_vDec`). |
| `StringDiagrams.Examples.OddTemperleyLieb.SKar`, `…Splitting` | Theorem A.3 (semisimplicity, additive form): in `SKar(STL(δ))` the objects `P n a = (f_n)^a_a` (`jwObj`) have `End = k` (`jwObj_isSchur`, `id_jwObj_ne_zero`) and no morphisms between distinct ones (`hom_jwObj_eq_zero`), and every object is isomorphic to a finite direct sum of them (`exists_iso_bsum_jwObj`, via splitting retracts of sums of Schur objects, `iso_bsum_of_retract`). |
| `StringDiagrams.Examples.OddTemperleyLieb.KZero`, `…KZeroRing`, `…KZeroIso` | Theorem A.3 = Theorem 1.18 (the Grothendieck ring): the classes `[P n b]` form a `ℤ`-basis of `K₀` (`basisK₀`, via multiplicities `multiplicity`); `1 = [P 0 0]`, `[P n 1] = π [P n 0]`, `P (N+1) 0 ⊗ P 1 0 ≅ P (N+2) 0 ⊞ P N 1` (`one_eq_classJw`, `classJw_one`, `tensorIso`); the ring isomorphism `K₀(SKar(STL(δ))) ≅` the subring of `Zπ[x, x⁻¹]` spanned by `[n+1]_{x,π}`, `π[n+1]_{x,π}`, sending `[P n b] ↦ π^b [n+1]_{x,π}` (`K₀ToLZ`, `K₀ToLZ_injective`, `range_K₀ToLZ`, `K₀Equiv`), whose basis elements are linearly independent (`linearIndependent_genL`). |
| `StringDiagrams.Examples.OddTemperleyLieb.KZeroPoly` | Example 1.17(iii): `K₀(SKar(STL(δ)))` is a polynomial algebra over `Zπ` generated by the class of the generating object (`K₀Poly : K₀ ≃ₐ[Zπ] Zπ[X]`, `K₀Poly_classJw_one_zero`), via injectivity of `X ↦ [2]_{x,π}` (`aeval_brk_two_injective`) and `range_aeval_brk_two`. |

**Erratum ((1.12)).** The printed identity `Σ_{n≥0} [n]_{x,π} tⁿ = 1/(1 - [2]_{x,π} t + π t²)` has left side with constant term `[0] = 0`; the correct statement is `Σ_{n≥0} [n+1]_{x,π} tⁿ = 1/(1 - [2]_{x,π} t + π t²)` (`qbracket_genFun_mul`), equivalently `Σ_{n≥0} [n]_{x,π} tⁿ = t/(1 - [2]_{x,π} t + π t²)` (`qbracket_genFun_mul'`).

Not formalized: that `SKar(STL(δ))` is an *abelian* category (Mathlib has no abelian structure on idempotent completions; the additive semisimplicity above is proved), and the comparison with `U_q(osp(1|2))` at the end of Appendix A (it relies on Clark–Wang).

Application-specific presentations are developed in downstream repositories that depend on this library.

All results are free of `sorry`; every declaration in the package depends on at most the axioms `propext`, `Classical.choice`, and `Quot.sound`.

## Building

Requires [elan](https://github.com/leanprover/elan). Toolchain
`leanprover/lean4:v4.19.0` and Mathlib `c44e0c8ee63ca166450922a373c7409c5d26b00b` are
pinned.

```sh
lake exe cache get
lake build
```

## Tactics

`StringDiagrams.Tactic.WordRw` (generic, for any ring): `word_rw [e₁, ← e₂]` rewrites with equations between words of generators modulo associativity, inside linear combinations, with side conditions discharged by `omega` (configurable); `word_comm [c₁, …]` decides equality of linear combinations of words modulo given (conditional) commutations, via normal forms in partially commutative monoids. Tests in `StringDiagrams.Tactic.Test` (generic rings, and the Temperley–Lieb, free-dots and odd-dots examples at symbolic positions).

## Notation and drawings

`StringDiagrams.DSL` provides a one-line notation for layered diagrams, e.g.
`[i i j] | x@0 ; psi@1 ; psi@0` (source object, then layers `generator@position` from
bottom to top), with a printer, a parser and the round-trip theorem `DSL.parse_print`.
`StringDiagrams.Render` draws diagrams as SVG (embedding the notation as metadata) and
TikZ. Drawings are presentation only; they are not evidence for any statement. Example
drawings are written to `out/` by

```sh
lake env lean --run scripts/render_examples.lean
```

## License

Released under the Apache License 2.0; see [`LICENSE`](LICENSE).
